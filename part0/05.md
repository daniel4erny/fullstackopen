flowchart TD
    client1(["GET on /exampleapp/spa"]) --> server1["server sends spa.html"]

    server1 --> client3["Browser sees spa.css and spa.js in sources and sends GET on the file paths"]
    client3 --> server2["server returns the .css and .js file"]
    server2 --> client4["browser executes .js code that fetches data.json"]

    client4 --> server3["server sends the .json file"]

    server3 --> client5["browser calls rendering function"]
