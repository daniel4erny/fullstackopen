flowchart TD
    client1(["user writes note and clicks Save"]) --> client2["browser's JS prevents default form submission, creates new note object and adds it to the notes array, then re-renders the notes list"]

    client2 --> client3["browser sends an AJAX HTTP POST to /exampleapp/new_note_spa with the new note as JSON"]
    client3 --> server1["server saves the note and responds with status code 201 Created. Client now knows that the note is correctly saved, so it only renders the notes with the new note. "]
